function helloReducer(context: Object, action: string) {
  return null;
}

// TODO: Determine what I'm passing in to define context for TS
// function updateSimpleField(context: Object, fieldName: string, value: string | number) {
//   context.setState({ [fieldName]: value });
// }

// function toggleField(fieldName: string) {
//   context.setState({ [fieldName]: !this.state[fieldName] });
// }
